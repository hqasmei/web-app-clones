import React from "react";

import AboutSection from "@/components/truewind/about-section";

export const metadata = {
  title: "Truewind | About",
  description: "Truewind",
};

const AboutPage = () => {
  return (
    <section>
      <AboutSection />
    </section>
  );
};

export default AboutPage;
